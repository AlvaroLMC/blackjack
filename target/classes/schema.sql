
-- Tabla de jugadores
CREATE TABLE IF NOT EXISTS player (
                                      id BIGINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
                                      name VARCHAR(50) NOT NULL,
                                      player_wins_counter BIGINT DEFAULT 0 NOT NULL
);